import AdminPageSettings from "./AdminPageSettings";

const AdminContactSettings = () => {
  return <AdminPageSettings pageGroup="contact" pageTitle="إعدادات صفحة اتصل بنا" />;
};

export default AdminContactSettings;

